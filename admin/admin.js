const API="/v1/admin-web";const loginView=document.getElementById("loginView"),appView=document.getElementById("appView"),form=document.getElementById("loginForm"),email=document.getElementById("loginEmail"),password=document.getElementById("loginPassword"),totp=document.getElementById("loginTotp"),totpField=document.getElementById("totpField"),errorBox=document.getElementById("loginError"),submit=document.getElementById("loginSubmit"),account=document.getElementById("account");function showLogin(){appView.hidden=true;loginView.hidden=false}function showApp(s){loginView.hidden=true;appView.hidden=false;account.textContent=s.displayName||s.email||"Admin"}async function session(){try{const r=await fetch(API+"/auth/session",{credentials:"same-origin",cache:"no-store"});if(!r.ok)throw 0;showApp(await r.json())}catch{showLogin()}}form.addEventListener("submit",async e=>{e.preventDefault();errorBox.textContent="";submit.disabled=true;try{const r=await fetch(API+"/auth/login",{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:email.value.trim(),password:password.value,totpCode:totp.value.trim()})});const body=await r.json().catch(()=>({}));if(r.ok){password.value="";totp.value="";showApp(body);return}if(body.code==="ADMIN_TOTP_REQUIRED"){totpField.hidden=false;totp.required=true;totp.focus();errorBox.textContent="인증 앱의 6자리 코드를 입력해 주세요.";return}errorBox.textContent=body.message||"로그인에 실패했습니다."}catch{errorBox.textContent="서버에 연결할 수 없습니다."}finally{submit.disabled=false}});account.addEventListener("click",async()=>{try{await fetch(API+"/auth/logout",{method:"POST",credentials:"same-origin"});}finally{password.value="";totp.value="";totpField.hidden=true;showLogin()}});const d=document.getElementById("trialDialog");document.querySelector('[data-action="trial"]').addEventListener("click",()=>d.showModal());document.querySelector(".close").addEventListener("click",()=>d.close());session();if("serviceWorker"in navigator){window.addEventListener("load",()=>navigator.serviceWorker.register("/admin/sw.js"));}